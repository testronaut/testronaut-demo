import { expect, test } from '@testronaut/angular';
import { RecipeFilter } from './recipe-filter';

test.describe('RecipeFilter', () => {
  test.skip('triggers filterChange', async ({ page, mount }) => {
    throw new Error('🚧 Work in progress!');
  });
});
